{
	"name": "bolleje-temporarysecret-cdk",
	"version": "0.1.0",
	"bin": {
		"bolleje-temporarysecret-cdk": "bin/bolleje-temporarysecret-cdk.js"
	},
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"test": "jest",
		"cdk": "cdk",
		"deploy": "cd resources && npm run build && cd dist && zip -r ../dist.zip * && cd ../.. && cdk destroy --all -f && cdk synth && cdk deploy --all --require-approval='never'"
	},
	"devDependencies": {
		"@aws-sdk/client-dynamodb": "^3.473.0",
		"@aws-sdk/lib-dynamodb": "^3.473.0",
		"@types/jest": "^29.5.8",
		"@types/node": "20.9.1",
		"aws-cdk": "2.114.1",
		"jest": "^29.7.0",
		"ts-jest": "^29.1.1",
		"ts-node": "^10.9.1",
		"typescript": "~5.2.2"
	},
	"dependencies": {
		"aws-cdk-lib": "2.114.1",
		"constructs": "^10.0.0",
		"source-map-support": "^0.5.21"
	}
}
