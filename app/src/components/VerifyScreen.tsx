import React from "react";

type VerifyScreenProps = {
	callback: () => void;
};

const VerifyScreen = () => {
	return <div className="fixed top-0 right-0 left-0 bottom-0 bg-red-500"></div>;
};

export default VerifyScreen;
